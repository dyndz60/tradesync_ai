name: tradesync_ai
description: منصة الشراكة وتجارة الجملة للمقاول الذاتي الجزائري
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # واجهة المستخدم والأيقونات
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  
  # إدارة الحالة (State Management)
  flutter_riverpod: ^2.5.1
  
  # قاعدة البيانات (معطلة حالياً حتى نقوم بربطها)
  # supabase_flutter: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # قم بإزالة علامة # أدناه إذا أضفت مجلد صور لاحقاً
  # assets:
  #   - assets/images/
